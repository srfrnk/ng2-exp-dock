{
  "version": "2",
  "services": {
    "client": {
      "build": "../client",
      "ports": [
        "80:80"
      ],
      "environment": {
        "API_HOST": "localhost",
        "API_PORT": "8080"
      }
    },
    "api": {
      "build": "../api",
      "ports": [
        "8080:3000"
      ],
      "environment": {
      }
    }
  }
}